# Odyssey Trading Agent — dependencies
# Install with: pip install -r requirements.txt

# ── Core ──────────────────────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.7,<3

# ── Numerical / Data ──────────────────────────────────────────────────────────
numpy>=1.26
pandas>=2.2
scipy>=1.12

# ── Graph ─────────────────────────────────────────────────────────────────────
networkx>=3.3

# ── RL / Gymnasium ────────────────────────────────────────────────────────────
gymnasium>=0.29.0     # or `gym>=0.26` as fallback

# ── DSPy (LLM agent) ──────────────────────────────────────────────────────────
# dspy-ai>=2.4          # uncomment once LLM keys are configured
# openai>=1.30          # required backend for DSPy

# ── Optional: live data ───────────────────────────────────────────────────────
# yfinance>=0.2.38      # pull OHLCV from Yahoo Finance
# ccxt>=4.3             # crypto/forex exchange connectors

# ── Optional: visualisation ───────────────────────────────────────────────────
# plotly>=5.20
# matplotlib>=3.8

# ── Dev / Testing ─────────────────────────────────────────────────────────────
httpx>=0.27           # async HTTP client for API tests
pytest>=8.0
pytest-asyncio>=0.23
